% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tiers.R
\name{simulate_planted}
\alias{simulate_planted}
\title{Simulate a matrix with a planted programme}
\usage{
simulate_planted(
  n_cells = 600,
  n_genes = 4000,
  n_target = 8,
  median_detected = 430,
  detection = 0.05,
  effect = 0.6,
  depth_programme_loading = 0,
  rank_frac = 0.05,
  seed = 0
)
}
\arguments{
\item{n_cells, n_genes, n_target}{Integers.}

\item{median_detected}{Median genes detected per cell.}

\item{detection}{Detection rate of a target gene at typical depth.}

\item{effect}{Log-scale effect of the programme on the target genes; zero is
the null.}

\item{depth_programme_loading}{How strongly depth tracks the programme.}

\item{rank_frac}{Rank ceiling as a fraction of `n_genes`; `max_rank` is
`ceiling(rank_frac * n_genes)`.  It is the regime knob: the tie-break
channel opens when `max_rank` exceeds the number of genes a cell detects.}

\item{seed}{Integer.}
}
\value{
A list with `matrix`, `genes`, `target`, `programme`, `depth` and
  `max_rank`.
}
\description{
Three things are generated jointly: a depth axis that depends on the latent
programme, a target gene set whose expression depends on depth, and a
background with enough gene-activity spread that an expression-matched null
has candidates to draw from.
}
