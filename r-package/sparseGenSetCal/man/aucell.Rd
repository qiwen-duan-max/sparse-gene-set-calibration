% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{aucell}
\alias{aucell}
\title{AUCell scores}
\usage{
aucell(cache, genes, max_rank = NULL)
}
\arguments{
\item{cache}{A `RankCache`.}

\item{genes}{Character vector.  Genes absent from the cache are dropped.}

\item{max_rank}{Rank ceiling; defaults to `ceil(0.05 * cache$n_genes_total)`.}
}
\value{
Numeric vector of scores in \eqn{[0, 1]}, one per cell.
}
\description{
Area under the recovery curve over the top `max_rank` ranks, normalised by
`length(genes) * max_rank`.
}
\examples{
X <- make_synthetic_matrix(n_cells = 100, n_genes = 500, seed = 2)
cache <- rank_cache(X$matrix, X$genes, ceiling = 40)
head(aucell(cache, X$genes[1:10]))

}
