% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{cutpoint_fpr}
\alias{cutpoint_fpr}
\title{False-positive rate of a cutpoint rule under a true null}
\usage{
cutpoint_fpr(
  score,
  outcome,
  n_perm = 1000,
  method = "optimum",
  pre_specified = NULL,
  alpha = ALPHA,
  seed = 0
)
}
\arguments{
\item{score}{Numeric vector.}

\item{outcome}{Logical vector.}

\item{n_perm}{Number of permutations.}

\item{method}{`"optimum"` re-selects the best cutpoint in every permutation;
`"median"` uses the median of the score, which does not depend on the
outcome; `"pre_specified"` uses `pre_specified`.}

\item{pre_specified}{A fixed cutpoint chosen without reference to the outcome.}

\item{alpha}{Nominal level.}

\item{seed}{Integer.}
}
\value{
A list with `fpr`, `n_perm`, `method`, and the observed split P value
  and cutpoint.
}
\description{
The outcome is permuted `n_perm` times, the rule is re-applied each time, and
the fraction of permutations declared significant at `alpha` is returned.
Under a valid rule this is `alpha`; the whole point is that an
outcome-chosen cutpoint is not a valid rule.
}
\examples{
set.seed(4)
score <- rnorm(300)
outcome <- score > 0
cutpoint_fpr(score, outcome, n_perm = 100, method = "median")$fpr

}
