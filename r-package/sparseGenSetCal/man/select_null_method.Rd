% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience.R
\name{select_null_method}
\alias{select_null_method}
\title{The checklist's decision, from quantities the caller already measured}
\usage{
select_null_method(
  tie_break_share = NULL,
  implied_rho_abs = NULL,
  chance_band = NULL,
  codetection_gap = NULL,
  axis_depth_rho = NULL,
  config = NULL,
  thresholds = NULL
)
}
\arguments{
\item{tie_break_share}{Step 1's exact quantity: the share of the set's
inclusion rate that comes from tie-breaking.}

\item{implied_rho_abs}{The magnitude of the pre-flight implied product.}

\item{chance_band}{The chance band to read the implied product against
(\code{\link{preflight}} returns it as \code{chance_band}).  Without it
the pre-flight step is reported as unevaluated.}

\item{codetection_gap}{The achieved co-detection gap of the best matched
draw.  A negative gap means the draws are more co-detected than the
query and never flags.}

\item{axis_depth_rho}{The correlation of the tested axis with the cells'
detected-gene count.}

\item{config, thresholds}{Overrides for the package defaults, as elsewhere
in the framework.}
}
\value{
A list with \code{recommended_action} (one of
  \code{conventional_null_ok}, \code{expression_matched_null},
  \code{refuse_not_identifiable}, \code{refuse_no_null_family_safe}),
  \code{steps} reporting what each supplied quantity said,
  \code{thresholds_applied} echoing the constants used, and \code{notes}
  carrying anything a reader of the output should know.
}
\description{
The checklist's decision, from quantities the caller already measured
}
\examples{
select_null_method(tie_break_share = 0.8, implied_rho_abs = 0.01,
                   chance_band = 0.05)
}
