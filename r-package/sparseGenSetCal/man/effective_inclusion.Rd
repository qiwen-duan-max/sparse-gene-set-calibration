% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theory.R
\name{effective_inclusion}
\alias{effective_inclusion}
\title{Inclusion probability of a gene under the tie-break, equation (3)}
\usage{
effective_inclusion(detection, depth, max_rank, n_genes)
}
\arguments{
\item{detection}{Numeric vector of per-gene detection rates \eqn{p}.}

\item{depth}{Integer vector of \eqn{D_c}, one entry per cell.}

\item{max_rank}{Integer `m`.}

\item{n_genes}{Integer `G`.}
}
\value{
Numeric vector with one entry per gene, averaged over the cells'
  depths.
}
\description{
The probability that a gene enters the top `max_rank` at all -- counted, or
placed there by the tie-break failing that -- given its detection rate and
the depths of the cells it is measured in.
}
\details{
This is the quantity an expression-matched null has to reproduce, and the
reason matching on \eqn{p} alone is not enough: two genes with the same
\eqn{p} have different inclusion when they are expressed in cells of
different depth, and depth is where the confound lives.
}
